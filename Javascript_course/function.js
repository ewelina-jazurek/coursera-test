
function addNumbers(arg1, arg2) {
    return arg1 + arg2;
}

const result = addNumbers(2, 2);

console.log(result);




/**
 * Convert minutes into seconds
 * @param {number} minutes 
 * @returns number with seconds
 */
function convertMinutesIntoSeconds(minutes) {
    return minutes * 60;
}


const seconds = convertMinutes(5);

console.log(seconds);



// Power Calculator
function powerCalculator(num1, num2) {
return num1 * num2; 

}

const circuitPower = powerCalculator(230,10);

console.log(circuitPower); 



// Return the Next Number from the Integer Passed
// Create a function that takes a number as an argument, increments the number by +1 and returns the result.


